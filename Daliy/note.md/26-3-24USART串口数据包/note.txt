	数据包的作用：把一个个单独的数据打包起来，方便我们进行多字节的数据通信，把属于同一批
的数据进行打包和分割方便接收方进行识别。
	串口数据包通常使用的是额外添加包头包尾这种方式。
	本小节规定HEX数据包格式位：固定包长，含包头包尾，其中包头为FF，载荷数据固定为4字节，
包尾为FE。
	在定义状态机时，else不能去掉。如果只用三个并列的if可能在状态转移时出现问题。保证条件
进来时只能选择执行其中的一个代码
	文本数据包：可变包长，包包头包尾，以@符号位包头，两个换行符('\r''\n')为包尾，载荷字符
数不固定